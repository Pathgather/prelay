# frozen_string_literal: true

require 'spec_helper'

class ArgumentTypeMutationSpec < PrelaySpec
  it "should accept arguments of all the possible types, including timestamp, and arrays thereof"

  it "should support optional and required arguments"

  it "should recursively symbolize arguments"
end
