# frozen_string_literal: true

require 'spec_helper'

class AttributeTypeSpec < PrelaySpec
  it "should support all of the built-in types and arrays of them"

  it "should support custom GraphQL types, and arrays of them"
end
