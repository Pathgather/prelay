# frozen_string_literal: true

require 'spec_helper'

class OneToOneInterfaceAssociationSpec < PrelaySpec
  it "should support fetching an associated item through a one-to-one association to an interface"
end
