# frozen_string_literal: true

require 'spec_helper'

class ConnectionSpec < PrelaySpec
  it "should give the given query a sensible default description"

  it "should give the query the appropriate connection type as a return type"
end
